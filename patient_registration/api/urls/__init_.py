# from django.urls import path, include

# from ..urls import (
#                     category_url,
#                     doctor_url,
#                     patient_url,
#                     summary_url,
#                     visit_url,
# )

# urlpatterns = [
#     path('category/', include(category_url)),
#     path('doctor/', include(doctor_url)),
#     path('patient/', include(patient_url)),
#     path('summary/', include(summary_url)),
#     path('visit/', include(visit_url)),
# ]